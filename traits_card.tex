\documentclass{article}
\usepackage{tikz}
\usepackage{fontspec}
\setmainfont{Pacifico}

\usepackage{background}

% Background settings
\backgroundsetup{
  scale=1,
  color=black,
  opacity=0.8,
  angle=0,
  position=current page.center,
  contents={%
    \includegraphics[width=\paperwidth,height=\paperheight]{Background.png}
  },
  pages=all, % Apply background to all pages
}

\begin{document}
\pagestyle{empty}
\large
\begin{itemize}
  \setlength{\itemsep}{-8pt}   % Decreases space between items
  \setlength{\parsep}{0pt}      % Removes space between paragraphs within an item
  \setlength{\topsep}{0pt}      % Removes space at the beginning of the list
  \setlength{\partopsep}{0pt}   % Removes extra space before the first item
  \input{traits.tex}
\end{itemize}

\end{document}
